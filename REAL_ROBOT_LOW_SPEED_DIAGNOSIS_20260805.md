# B2RM 低速真机诊断与下一轮训练修改

日期：2026-08-05  
训练分支：`real-deploy`  
部署权重：`logs/instinct_rl/b2rm_velocity/20260804_233407/exported/actor.onnx`  
部署记录仓库：`HaoZhangRobotics/deploy`，提交 `adec057`（实机日志上传）。

## 1. 已确认正确的部署合同

本轮不是旧版 408 维或足端接触输入的部署。真实 C++ 适配器和当前 ONNX 已核对：

```text
ONNX:        [1, 376] -> [1, 12]
history:     8 frames, term-major, 47 values/frame
actor terms: q_rel[12], dq[12], base_ang_vel[3], projected_gravity[3],
             command[3], raw_last_action[12], gait_sin_cos[2]
action:      target2 + 0.3 * clip(raw_action, -1, 1)
policy PD:   Kp=250, Kd=5
stand PD:    Kp=1000, Kd=10
default:     hip=0, thigh=0.67, calf=-1.30
```

关节顺序、SDK 映射、历史拼接、动作缩放和 ONNX 离线推理均已验证。足端原始力和估计力保留在日志中，但不再作为 Actor 输入；这符合本任务训练配置。

## 2. 四次真机结果

日志路径在 deploy 仓库：`b2rm_velocity_history_cpp/logs/`。

| 日志目录 | 命令 (m/s, rad/s) | 接管后的结果 |
|---|---|---|
| `20260805_150643_history` | `0, 0, 0` | 最大前后倾约 3.3 度；无明显失稳。 |
| `20260805_150715_history` | `0, 0, 0` | 最大前后倾约 3.2 度，后续回到约 0.5 度；约 24 秒稳定。人工向前轻推后仍能恢复保持。 |
| `20260805_150842_history` | `0.05, 0, 0` | 接管约 0.6 秒后前后倾约 14 度，并有横向晃动。原始动作最大 2.09，最多 5 个关节被裁剪。 |
| `20260805_150918_history` | `0.10, 0, 0` | 持续原地晃动且横滚放大，最终横滚约 32 度。原始动作最大 2.26，最多 6 个关节被裁剪。 |

结论：零命令站立已有局部恢复能力；低速行走模式不稳定。问题不是策略接管前的 target2、PD 切换或 376 维部署合同，而是低速步态本身。

## 3. 为什么会出现“原地晃”

当前 `b2rm_velocity_handoff_cfg.py` 将 `0.05` 同时作为多个二值门槛：

```text
|command| < 0.05: gait phase 固定 [0, 1]，零命令站立奖励生效
|command| >= 0.05: 0.7 s gait phase 开始，trot 对角触地、摆腿速度、
                   feet_air_time、feet_height 等行走项立即生效
```

测试 `0.05 m/s` 恰好位于这个模式切换点。策略需要跟踪极低前进速度，却同时被鼓励做完整对角摆腿：

- `trot_phase_contact` 是开关式奖励，移动时不按速度降低；
- `trot_phase_foot_velocity` 的 `min_swing_speed=0.10`，对 `0.05 m/s` 指令仍要求明显摆腿；
- `feet_height` 的目标高度为 `0.15 m`，也只由阈值开关；
- 速度跟踪和步态目标发生竞争，导致机器人更像原地踏步而不是低速前进；
- 原地大幅换腿使重心在前后和左右摆动，真实机器人上表现为后倾、横滚和晃动。

动作日志显示行走时多关节长期超出 `[-1, 1]` 并被部署器裁剪。该裁剪与训练处理后的目标范围相匹配，因此不是额外的部署误差；它说明当前策略本身频繁要求边界动作，步态缺少余量。

## 4. 人工前推后能恢复是什么意思

Actor 仍观测八帧关节位置、关节速度、机身角速度和重力方向。人工推力改变了这些量，并把重心带回了零命令策略的一个局部稳定区域；日志中的零命令运行最终确实回到接近竖直。

这不表示策略可在任意姿态恢复，也不能证明它“回到训练分布”。它只证明当前站立策略存在有限的稳定盆地，低速 gait 分支离开该盆地后无法可靠拉回。

## 5. 结构性限制：Actor 没有 base_lin_vel

`base_lin_vel = [vx, vy, vz]` 是机身坐标系的实际线速度。Critic 有仿真的 `base_lin_vel`，Actor 没有，因为真机没有已验证可信的线速度估计。

Actor 知道命令是 `vx=0.05`，但不能直接知道真机是否已经以 `0.05 m/s` 前进，只能从关节、IMU 和动作历史间接推断。因此它可以做姿态恢复，但低速速度跟踪和抗外扰步态比有可靠速度观测的策略更困难。

不要为了补这个输入而直接使用当前 foot-force stance estimator：实测 foot force 没有可靠接触阈值，估计速度不可信。下一轮先优化无速度 Actor 的低速 gait；后续若有可靠 VIO、轮速或状态估计，再考虑增加经过同样训练和部署验证的速度输入。

## 6. 下一轮训练修改，按优先级执行

### P0：把站立到行走改为连续过渡

必须消除 `0.05` 的二值突变。训练和部署须使用同一个连续 gait activation，例如根据前进命令大小定义 `g = clamp((|vx| - 0.02) / 0.13, 0, 1)`：

- `gait_sin_cos` 可以从冻结状态平滑进入运行，而不是刚过 0.05 就完整启动；
- trot contact、swing velocity、feet air-time、feet height 的权重乘以 `g`；
- 摆腿速度和抬脚高度目标也乘以 `g`；
- 部署端命令应从 0 平滑 ramp 到目标速度，训练里也要出现同样的 command ramp。

不要只在部署端加 ramp 而训练保持跳变，否则又引入新的训练-部署差异。

### P1：重采样低速命令

当前移动速度均匀采样 `0.05~0.50 m/s`，低速段比例太小。建议第一阶段：

```text
60%: vx in [0.03, 0.15] m/s
25%: vx in [0.15, 0.30] m/s
15%: vx in [0.30, 0.50] m/s
```

保留约 30% 零命令环境。测试和训练都从 `0 -> 0.03 -> 0.05 -> 0.10` 的渐进命令开始，不再直接将站立状态切到全幅 gait。

### P2：降低低速 gait 的摆腿要求

- 将 `min_swing_speed` 从 `0.10` 降到约 `0.03~0.05`，或完全改为与速度成比例；
- 将 `feet_height` 从固定 `0.15 m` 改为随 `g` 从接近 0 平滑增加；
- 对 `trot_phase_contact` 使用速度加权，低速允许更多四足支撑时间，不能强制完整对角腾空；
- 保持 gait period，但低速不要追求完整 0.7 s 的高摆幅 trot。

目标是先得到小步、四足支撑占比更高的低速稳定行走，而不是视觉上“像跑步”的原地换腿。

### P3：明确惩罚饱和和横滚

实机行走日志已有 5 到 6 个动作同时裁剪的证据。建议从保守值开始：

- `action_saturation`：权重由 `-0.20` 增至约 `-1.0`，soft limit 从 `0.8` 降至约 `0.7`；
- 增大 `action_rate_l2` 的惩罚，抑制相邻 50 Hz 策略输出的突跳；
- 加强 `flat_orientation_l2` 和 `ang_vel_xy_l2`，优先压制 roll/pitch；
- 增加左右对称约束或对左右腿目标差的正则，特别是纯前进命令时；
- 增加 base 横向速度、横向漂移或左右脚受力/支撑不均衡的惩罚（仿真接触可用）。

应逐项调权重并用仿真/日志检查，而不是一次性把所有惩罚调得很大，以免策略只学会僵硬站立。

### P4：专门训练接管和抗扰恢复

训练中增加以下初始和过程扰动：

- target2 附近的关节位置、关节速度、roll/pitch 初值随机化；
- 从站立历史缓冲开始，再将命令平滑升到低速行走；
- 在站立及 `0.03~0.15 m/s` 行走阶段加入小的前后/左右 base 速度或外力脉冲；
- 对扰动后的恢复时间、最大 roll/pitch、动作饱和量建立明确惩罚。

这会让“手推后恢复”从偶然的局部能力，变为可量化训练目标。

## 7. 下一版验收顺序

1. 仿真：零速度站立、`0.03`、`0.05`、`0.10`，记录 roll/pitch、动作裁剪比例和真实 `base_lin_vel` 跟踪。
2. 仿真：站立到 `0.05` 的 ramp，以及小外力后的恢复。
3. 真机吊装：零速度和 `0.03`，只验证动作幅度、姿态和日志；不追求位移。
4. 真机落地：`0.03`，再 `0.05`，每档稳定后才进入下一档。
5. 当 `0.05` 不再出现持续饱和和横滚后，才测试 `0.10`。

## 8. 不应作为下一步的操作

- 不要提高部署 PD 来掩盖 gait 不稳定；当前问题主要是策略目标序列和奖励冲突。
- 不要恢复 Actor 足端 contact 输入；本机标定表明该数据不可分离且会引入错误观测。
- 不要把当前 stance-foot 速度估计直接作为 `base_lin_vel` Actor 输入。
- 不要继续直接测试更高速度；`0.10` 已显示该策略尚未通过低速阶段。
